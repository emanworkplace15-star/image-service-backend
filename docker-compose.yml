services:
  postgres:
    image: postgres:16-alpine
    container_name: image-service-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: imagedb
    ports:
      - '5432:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U postgres -d imagedb']
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  pgdata:
